server:
  port: 8083

spring:
  application:
    name: tournament-service
  datasource:
    url: jdbc:mysql://localhost:3306/esports_tournament_db?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: contraseña
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect